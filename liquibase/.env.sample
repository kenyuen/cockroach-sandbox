# Liquibase-specific environment sample.
# Copy to liquibase/.env and edit before running Maven-based Liquibase commands.
# NEVER commit liquibase/.env (ignored by git).

# Primary JDBC URL for Liquibase (points at the movr database once it exists).
LIQUIBASE_URL=jdbc:postgresql://localhost:26257/movr?sslmode=disable
LIQUIBASE_USERNAME=root
LIQUIBASE_PASSWORD=
# Runtime password used when Liquibase creates the movr application role.
MOVR_USER_PASSWORD=changeme # TODO: replace with a per-developer secret before running

# Bootstrap URL hits defaultdb so we can create movr before Liquibase runs.
LIQUIBASE_BOOTSTRAP_URL=jdbc:postgresql://localhost:26257/defaultdb?sslmode=disable
# Override if the cockroach binary lives elsewhere.
COCKROACH_BINARY=cockroach
# TODO: provide secure-mode example (certs, sslmode=require) for shared environments.
